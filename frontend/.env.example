# Browser API base. Leave blank to use the frontend's same-origin proxy.
# Static hosting requires the HTTPS base URL of the separately hosted Python API.
VITE_API_URL=
# Used only by the local frontend proxy. No secrets are embedded in the browser.
PYTHON_BACKEND_URL=http://127.0.0.1:8000
PORT=3000
